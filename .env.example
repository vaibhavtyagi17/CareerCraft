# Example .env file for CareerCraft
# Copy to .env and replace placeholder values before running locally

PORT=5000
JWT_SECRET=__GENERATE_A_SECURE_SECRET_AND_PASTE_IT_HERE__
NODE_ENV=production

# If using Postgres, set DATABASE_URL in this format:
# DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DATABASE
